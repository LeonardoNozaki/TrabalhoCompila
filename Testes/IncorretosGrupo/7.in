function retornos (a : Int, b : Int) -> Int {
	if a > 0{
		return "String"; //Tipo de retorno errado
	}
	else {
		return true; //Tipo de retorno errado
		return false; //Tipo de retorno errado
	}
	
	return c; //Retorno errado, variavel nao existe

	return a;	
	return -10;
}

function main{
	var Int : a;
	a = true + 10; //Erro, soma de boolean com int
	a = "String" and 10; //Erro, operacao logica de string com int
	a = false and true and "String" //Erro, operacao logica de string com boolean
}
